import 'package:flutter/material.dart';

class ErrorWidget1 extends StatelessWidget {
  const ErrorWidget1({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Error'),
    );
  }
}
